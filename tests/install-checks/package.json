{
  "name": "install-checks-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-app PWA install checks for grocery, pool, vehicle and bank-bonus (GAP-W6).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
