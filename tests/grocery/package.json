{
  "name": "grocery-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright regression rig for the Grocery Tracker app (PRD S0).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
